// Atlas 1 — console bootstrap.
// M1: just log version. Shell, REPL, and plugin loader land in M2/M3.

const VERSION = "0.1.0";

window.addEventListener("DOMContentLoaded", () => {
  // eslint-disable-next-line no-console
  console.log(`Atlas 1 · v${VERSION} · booted`);
});

export {};
